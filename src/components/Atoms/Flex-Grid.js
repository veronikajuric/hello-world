import React from 'react';
import styled from 'styled-components';
import { flex, grid } from '../../styles/flex-grid';

export const Flex = styled.div`
	${flex};
`;

export const Grid = styled.div`
	${grid};
`;
